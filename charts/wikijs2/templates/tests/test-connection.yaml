apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wikijs2.fullname" . }}-test-connection"
  labels:
    {{- include "wikijs2.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wikijs2.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
